apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-green
  namespace: webapp-bg
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp
      version: green
  template:
    metadata:
      labels:
        app: webapp
        version: green
    spec:
      containers:
        - name: webapp
          image: smitdarji/k8s:gg_l.65_4b4350f
          ports:
            - containerPort: 80
