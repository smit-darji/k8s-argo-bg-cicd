apiVersion: v1
kind: Service
metadata:
  name: webapp-bg-stable
  namespace: webapp-bg
spec:
  selector:
    app: webapp
  ports:
    - port: 80
      targetPort: 80
  type: NodePort
---
apiVersion: v1
kind: Service
metadata:
  name: webapp-bg-preview
  namespace: webapp-bg
spec:
  selector:
    app: webapp
  ports:
    - port: 80
      targetPort: 80
  type: NodePort
