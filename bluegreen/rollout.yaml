apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: webapp-bg
  namespace: webapp-bg
spec:
  replicas: 2
  revisionHistoryLimit: 2
  selector:
    matchLabels:
      app: webapp
  strategy:
    blueGreen:
      activeService: webapp-bg-stable
      previewService: webapp-bg-preview
      autoPromotionEnabled: true
      autoPromotionSeconds: 180
  template:
    metadata:
      labels:
        app: webapp
        version: blue  # 👈 dynamically updated by CI
    spec:
      containers:
        - name: webapp
          image: smitdarji/k8s:v.b.1.0  # 👈 dynamically updated by CI
          ports:
            - containerPort: 80
